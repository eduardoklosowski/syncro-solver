{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "syncro-solver",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build",
                    "--package=syncro-solver",
                    "--bin=syncro-solver"
                ],
                "filter": {
                    "kind": "bin",
                    "name": "syncro-solver"
                }
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}
